#include <stdio.h>
//#include <stdlib.h>
//#include <errno.h>
//#include <string.h>
//#include <unistd.h>
#include <spawn.h>
#include <sys/types.h>
//#include <sys/stat.h>
#include <sys/wait.h>

// sets up environment variable
extern char **environ;

void spawn_and_wait(char **args) {
    // TODO: set things up to spawn a process, then wait for it to finish
    pid_t pid;
    int status;
}

int main(void) {
    // use whereis to find the locations of commands
    // TODO: call spawn and wait to run the commands
    // TODO: make this call print_diary as a separate process
    return 0;
}